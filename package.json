{
  "name": "virtworker",
  "productName": "VirtWorker",
  "version": "0.1.0-Alpha",
  "description": "VirtWorker - 自定义创建数字员工 Agent 并实现协同工作的 Windows 桌面应用",
  "author": "VirtWorker Team",
  "license": "MIT",
  "main": "main/main.js",
  "scripts": {
    "prestart": "node scripts/build-renderer.js",
    "start": "electron .",
    "predev": "node scripts/build-renderer.js",
    "dev": "electron . --dev",
    "build:renderer": "node scripts/build-renderer.js",
    "watch:renderer": "node scripts/build-renderer.js --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint main preload renderer scripts",
    "rebuild": "electron-rebuild",
    "prebuild": "node scripts/build-renderer.js",
    "build": "electron-builder --win",
    "build:dir": "electron-builder --win --dir",
    "build:portable": "electron-builder --win portable"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "vitest": "^5.0.1"
  },
  "build": {
    "appId": "com.virtworker.app",
    "productName": "VirtWorker",
    "copyright": "Copyright © 2026 VirtWorker",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main/**/*",
      "preload/**/*",
      "renderer/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "VirtWorker"
    },
    "portable": {
      "artifactName": "${productName}-${version}-Portable.${ext}"
    }
  },
  "allowScripts": {
    "electron@33.4.11": true
  }
}
